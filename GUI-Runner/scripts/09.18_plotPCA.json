{
  "name": "09.18-Deeptools: plotPCA",
  "script_path": "/GUI/conf/scripts/plotPCA.sh",
  "description": "Tool for generating a principal component analysis (PCA) plot from multiBamSummary or multiBigwigSummary output. By default, the loadings for each sample in each principal component is plotted. If the data is transposed, the projections of each sample on the requested principal components is plotted instead",
  "working_directory": "/root/workdir/data",
  "parameters": [
    {
      "name": "Input coverage",
      "param": "--corData",
      "description": "Coverage file (generated by multiBamSummary or multiBigwigSummary)",
      "required": true
    },
    {
      "name": "Output",
      "param": "-o",
      "description": "File name to save the plot to. The extension determines the file format. For example: pca.pdf will save the PCA plot in PDF format. The available options are: .png, .eps, .pdf and .svg. If this option is omitted, then you MUST specify –outFileNameData"
    },
    {
      "name": "Labels",
      "param": "-l",
      "description": "User defined labels instead of default labels from file names. Multiple labels have to be separated by spaces, e.g. –labels sample1 sample2 sample3"
    },
    {
      "name": "Title",
      "param": "-T",
      "description": "Title of the plot, to be printed on top of the generated image. Leave blank for no title"
    },
    {
      "name": "Other parameters",
      "description": "Other parameters"
    }
  ]
}
